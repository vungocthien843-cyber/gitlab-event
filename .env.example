# ============================================
# AI20K Project — Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# ---- LLM Configuration ----
OPENAI_API_KEY=sk-your-key-here
# Or use other providers:
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=

# ---- Database (Postgres — bắt buộc) ----
# Bảng `input_json` được SQLAlchemy tự tạo lúc khởi động, không cần chạy SQL tay.
# Phần `options=-csearch_path%3D<schema>` quyết định schema chứa bảng; thiếu nó
# thì app dùng DB_SCHEMA, mặc định `ai20k_db`.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require&options=-csearch_path%3Dai20k_db
# DB_SCHEMA=ai20k_db
# DB_ECHO=false

# Schema riêng cho pytest (tạo và xoá tự động, không đụng dữ liệu thật)
TEST_DB_SCHEMA=ai20k_db_test

# ---- Vector Store ----
CHROMA_PERSIST_DIR=./data/chroma
# Or Pinecone:
# PINECONE_API_KEY=
# PINECONE_ENVIRONMENT=

# ---- App Config ----
APP_ENV=development
APP_PORT=8000
APP_HOST=0.0.0.0
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ---- LangSmith (AI Logs — Deliverable #4) ----
LANGCHAIN_API_KEY=your-langsmith-key-here
LANGCHAIN_PROJECT=ai20k-agent
LANGCHAIN_TRACING_V2=true

# ---- Logging ----
LOG_LEVEL=INFO

# ---- AI Hook Logging (provided by instructor) ----
AI_LOG_SERVER=https://ai-logs.note.transformerlabs.ai/api/ingest
AI_LOG_API_KEY=f3p1dEOhD_z8qjjtbeCtb2t9ASMEliz-4juZfWe3ZPgCtPZp-LjWthBfeNU8d5dP
AI_LOG_DIR=.ai-log

# ---- Frontend (if separate) ----
# NEXT_PUBLIC_API_URL=http://localhost:8000
