# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# Catalog ingestion (YAML -> graph JSON)
pyyaml>=6.0
networkx>=3.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Database — Postgres (Neon). psycopg2-binary có sẵn wheel cho cp311 (Docker)
# lẫn cp314 (venv local) nên không cần build tools khi cài.
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.12
# alembic>=1.14.0

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
