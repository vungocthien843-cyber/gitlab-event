specVersion: vsf-idp.io/v2
# ═══════════════════════════════════════════════════════════════════════════
# FILE C — LỖI CÚ PHÁP YAML (tầng 1, FAIL-FAST)
# File này CỐ Ý VỠ cú pháp. Parser dừng NGAY ở khâu 1, không chạy validate.
# Chủ đích: test nhánh YAML_SYNTAX. Mọi lỗi tầng 2+ trong file KHÔNG được kiểm
# tới -- đó là lý do lỗi cú pháp phải để riêng một file.
#
# Lỗi cố ý:
#   [T1] indent sai + key trùng     -> YAML_SYNTAX
# ═══════════════════════════════════════════════════════════════════════════

metadata:
  domain: Test
  domain: Duplicated Domain Key
   system: bad-indent-here
  namespace: test

spec:
  type: worker
   id: broken
  name: File cú pháp vỡ
